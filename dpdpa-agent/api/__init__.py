"""
Veritas DPDPA Agent — Integration API Layer (Phase 5)
=========================================================
Kept as its own top-level package, separate from dashboard/ (which serves
the HTML/WebSocket dashboard) and evidence_store/ (storage), per the
plan's instruction not to blur the API layer and the storage layer
together. See api/integration.py for the actual routes.
"""
