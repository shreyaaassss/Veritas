# DPDPA Compliance Agent — Dependencies
# =======================================
# All phases implemented. Install all before running.

# --- Authentication ---
passlib[bcrypt]>=1.7.4          # Secure password hashing (bcrypt)
bcrypt>=3.2.0,<4.0.0            # passlib is incompatible with bcrypt>=4.0 (__about__ removed)
python-jose[cryptography]>=3.3.0  # JWT token signing/verification
python-multipart>=0.0.9         # Required for FastAPI form-based login

# --- Core API ---
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0
pydantic>=2.12.5,<3.0.0         # presidio-analyzer requires >=2.12.5
pydantic[email]
pyyaml>=6.0

# --- Testing ---
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0                   # FastAPI TestClient

# --- PII Detection ---
presidio-analyzer==2.2.364
presidio-anonymizer==2.2.364
spacy>=3.8.0,<4.0.0
# en_core_web_lg — install separately:
#   python -m spacy download en_core_web_lg
#   OR: pip install https://github.com/explosion/spacy-models/releases/download/en_core_web_lg-3.8.0/en_core_web_lg-3.8.0-py3-none-any.whl

# --- Phase 17: Database encryption at rest ---
cryptography>=3.0               # Fernet symmetric encryption for evidence store payload_json

# --- Phase 5: LLM Explainer ---
openai>=1.0.0                   # Used for LLM calls; falls back gracefully if no API key
# anthropic>=0.34.2             # Alternative — uncomment if using Claude directly

# --- HTTP client ---
requests>=2.31.0
